// VMA is header-only; its implementation must be compiled in exactly one
// translation unit. Kept alone in this file so the ~20k-line header is not
// re-parsed as part of the application code.
#define VMA_IMPLEMENTATION
#include <vk_mem_alloc.h>
